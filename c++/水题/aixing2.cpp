#include<bits/stdc++.h>
using namespace std; 
int main(){
	 for(int i;;i++)
	{
		cout<<"* * * * * * * * * * * *"<<endl<<"* * * ^ ^ * * ^ ^ * * *"<<endl<<"* * ^ ^ ^ * * ^ ^ ^ * *"<<endl<<"* * ^ ^ ^ ^ ^ ^ ^ ^ * *"<<endl<<"* * * ^ ^ ^ ^ ^ ^ * * *"<<endl<<"* * * * ^ ^ ^ ^ * * * *"<<endl<<"* * * * * ^ ^ * * * * *"<<endl;
	}
	return 0;
}
