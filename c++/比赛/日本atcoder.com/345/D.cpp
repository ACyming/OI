#include <bits/stdc++.h>
using namespace std;
int n, h, w;
int a[20], b[20];
void dfs(int x){

    
}
int main()
{
    cin >> n >> h >> w;
    for (int i = 1; i <= n; i++) {
        cin >> a[i] >> b[i];
    }
    dfs(1);
    return 0;
}