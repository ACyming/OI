#include <bits/stdc++.h>
#define int long long
using namespace std;
int k;
main()
{
    cin >> k;
    while (k--)
    {
        int n, d, e;
        cin >> n >> d >> e;
        int b = (-n - e * d + 2), c = n;
        d = b * b - 4 * c;
    }
    return 0;
}